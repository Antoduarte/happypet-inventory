.atl/*
.agent/*
.claude/*

node_modules/
.venv/
.env
staticfiles/
__pycache__/
*.py[cod]
*.egg-info

# Frontend build output is committed for client deployments that don't have Node.js.
# To regenerate locally, run: cd frontend && npm ci && npm run build
# backend/static/frontend_build/
